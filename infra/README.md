# Infra

Azure deployment configuration and scripts — populated in Phase 5.

Planned contents:
- `azure/`  — Azure CLI scripts for resource provisioning
- `bicep/`  — Bicep templates for Azure App Service, SQL, Blob Storage
- `.github/workflows/` — CI/CD pipelines (to be placed at repo root)
